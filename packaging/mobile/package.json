{
  "name": "starquest-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Capacitor shell for STARQUEST \u2014 Android APK and the iOS project.",
  "license": "MIT",
  "scripts": {
    "sync": "node ../../tools/make-dist.mjs www && cap sync",
    "add:android": "node ../../tools/make-dist.mjs www && cap add android && capacitor-assets generate --android && node ../../tools/patch-android.mjs",
    "add:ios": "node ../../tools/make-dist.mjs www && cap add ios && capacitor-assets generate --ios",
    "open:android": "cap open android",
    "open:ios": "cap open ios"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.2.0"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0"
  }
}
